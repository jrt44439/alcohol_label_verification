.venv/
__pycache__/
*.pyc
instance/
uploads/*
!uploads/.gitkeep
.pytest_cache/
*.sqlite3

# Standalone Windows build artifacts (see README.md's "Standalone Windows
# build" section) -- regenerated by packaging/stage_tesseract.py and
# PyInstaller, not checked in.
build/
dist/
packaging/tesseract/

# Code-signing certificate + private key (see packaging/sign_exe.ps1) --
# machine-local, never checked in.
packaging/codesign/
